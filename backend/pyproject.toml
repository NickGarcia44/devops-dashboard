[project]
name = "devops-dashboard"
version = "0.1.0"
description = "Dashboard para monitoramento DevOps"
readme = "../README.md"
requires-python = ">=3.14"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "pydantic",
    "psutil",
]